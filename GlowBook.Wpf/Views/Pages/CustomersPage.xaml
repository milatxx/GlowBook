<Page x:Class="GlowBook.Wpf.Views.Pages.CustomersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
      xmlns:vm="clr-namespace:GlowBook.Wpf.ViewModels"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance Type=vm:CustomersViewModel, IsDesignTimeCreatable=False}">
    
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
            <Button Content="Nieuw"       Margin="0,0,8,0" Command="{Binding AddCommand}"/>
            <Button Content="Opslaan"     Margin="0,0,8,0" Command="{Binding SaveCommand}"/>
            <Button Content="Verwijderen"                  Command="{Binding DeleteCommand}"/>
        </StackPanel>

        <DataGrid
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="False"
                  Margin="0,8,0,0">
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="Naam"
        Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Telefoon"
        Binding="{Binding Phone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="150"/>
                <DataGridTextColumn Header="Email"
        Binding="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="220"/>
                <DataGridTextColumn Header="Notities"
        Binding="{Binding Notes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="2*"/>
            </DataGrid.Columns>

        </DataGrid>
    </DockPanel>
</Page>
