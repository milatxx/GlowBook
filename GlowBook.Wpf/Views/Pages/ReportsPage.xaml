<Page x:Class="GlowBook.Wpf.Views.Pages.ReportsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:GlowBook.Wpf.ViewModels">
    <Page.DataContext>
    <vm:ReportsViewModel/>
    </Page.DataContext>


    <Grid Margin="24">
        <Border Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Rapportage" Style="{StaticResource CardTitle}" Margin="0,0,0,8"/>

                <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <TextBlock Text="Van:" Foreground="#FFEC628B" VerticalAlignment="Center"/>
                    <DatePicker x:Name="dpFrom" SelectedDate="{Binding From}"/>
                    <TextBlock Text="Tot:" Foreground="#FFEC628B" VerticalAlignment="Center"/>
                    <DatePicker x:Name="dpTo" SelectedDate="{Binding To}"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                    <Button Content="Exporteer CSV" Click="Csv_Click"/>
                    <Button Content="Exporteer PDF" Click="Pdf_Click"/>
                </StackPanel>
            </Grid>
        </Border>
        
    </Grid>
</Page>
